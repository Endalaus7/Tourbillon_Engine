#pragma once

#include "rhi_command.h"

namespace TourBillon
{
	void RHICommand::ExecuteAndDestruct(RHICommandList& CmdList)
	{
		//TCmd* ThisCmd = static_cast<TCmd*>(this);
		//ThisCmd->Execute(CmdList);
		//ThisCmd->~TCmd();
	}
}